# Requirements Ledger — Device B (Shell & Surface)

Rows `B1..Bn`. Concatenates with A's `A1..An` at convergence. Priority tags per BUILD_B §2.2.
Status column filled at convergence (2026-09-12 12:50 EDT): **62 SATISFIED, 2 PARTIAL (B41 teammate tap measurement, B59 level-1 of the ladder), 1 MISSING (B62 perception — no hardware)**. Evidence artifacts: `data/eval/2026-09-12/{p0_gate_report,p1_replay_report,p3_protocol_only_rehearsal,p3_fallback_ladder}.json`.

Scope (from the §2.4 hardware check, 2026-09-12 06:36 EDT): **P0 harness clauses, P3 in full, all content. P4–P6 OPEN — no camera, no fixtures.**

| id | requirement (testable condition) | priority | source (`file § heading`) | verified_by | owner | status |
|---|---|---|---|---|---|---|
| B1 | `import-linter` rejects a deliberately planted violation (a `state/` module importing `perception/`) and exits 0 without it | `[DEMO-CRITICAL]` | `38-workflow.md § P0 — Gate`; `39 § 7` | `lint-imports` exit≠0 with plant; `tests/integration/meta/test_layer_dag.py` | B | **SATISFIED** — `pyproject.toml` [tool.importlinter] rule 3; `tests/integration/meta/test_layer_dag.py::test_planted_violation_in_state_is_rejected`; `data/eval/2026-09-12/p0_gate_report.json` plant (a): exit 1 with plant, 0 after revert |
| B2 | Config validation rejects a deliberately malformed bundle with a specific error — never a silent default, never a partial load | `[DEMO-CRITICAL]` | `38 § P0 — Gate`; `31 § Validation`; `23 § P12` | `tests/integration/meta/test_config_rejects_malformed.py` | B | **SATISFIED** — `src/runtime/config.py:320 load`; `tests/integration/meta/test_config_rejects_malformed.py` (15 planted defects rejected, real bundle loads); p0 report plant (c) |
| B3 | `pip install -e .` installs core + runtime only; `perception` and `dev` are extras | `[DEMO-CRITICAL]` | `39 § 2 Dependencies` | `uv pip install -e .` then `import cv2` fails | B | **SATISFIED** — `pyproject.toml` dependencies vs `[project.optional-dependencies]`; `python -c 'import cv2'` → ModuleNotFoundError (p0 report) |
| B4 | Safety core importable, runnable, testable with `opencv` absent; nothing under `runtime/` imports `perception/` at module scope (lazy import inside the composition root only) | `[DEMO-CRITICAL]` | `39 § 2`; `32 § Principles 1–2`; `ADR-0001` | `pytest -q tests/integration` in env without cv2; grep `src/runtime` for top-level `src.perception` | B | **SATISFIED** — grep `cv2|opencv|mediapipe` in `src/**.py` = 0 code hits (one docstring); whole suite runs in a venv without opencv (p0/p1 reports) |
| B5 | Dependency set is exactly `39` §2 (pydantic ~2.9, pyyaml ~6.0, fastapi ~0.115, uvicorn ~0.32; extras opencv ~4.10, numpy ~2.1, mediapipe ~0.10; dev pytest, pytest-asyncio, hypothesis ~6, import-linter ~2.0, mypy ~1.11, ruff ~0.7). Nothing else; core-group additions need an ADR | `[DEMO-CRITICAL]` | `39 § 2`; `32 § Selection gate` | diff `pyproject.toml` against the table | B | **SATISFIED** — `pyproject.toml` = 39 §2 table; recorded deviations: `pydantic==2.13.5` exact (32), `uvicorn[standard]` extra (CG-B14), `types-PyYAML`+`httpx` dev-only; `requirements.lock` |
| B6 | Rule 1: `domain/` imports nothing internal | `[DEMO-CRITICAL]` | `30 § Dependency rules 1`; `22 § Dependency direction` | contract `domain imports nothing internal` | B | **SATISFIED** — `pyproject.toml` contract 'rule 1'; `lint-imports` 8 kept |
| B7 | Rule 2: `perception/` imports **only** `domain/` and `events/` | `[DEMO-CRITICAL]` | `30 § rule 2`; `21 § Non-obligations`; `22 § PerceptionPipeline` | contract `perception imports only domain and events` | B | **SATISFIED** — contract 'rule 2'; `src/perception/` is empty (no hardware) |
| B8 | Rule 3: `state/ risk/ policy/ orders/` never import `perception/ ui/ runtime/` | `[DEMO-CRITICAL]` | `30 § rule 3` | contract `core never imports the shell` | B | **SATISFIED** — contract 'rule 3'; plant rejected (B1) |
| B9 | Rule 4: `runtime/` is the only composition root — no other package imports `runtime/`; only `runtime/` imports `ui/`; only `runtime/`/`replay/` reach across layers | `[DEMO-CRITICAL]` | `30 § rule 4`; `22 § Dependency direction` | contract `runtime is the only composition root` | B | **SATISFIED** — contracts 'rule 4', '4b', '4c'; `src/runtime/app.py` is the only importer of `src.ui`/`src.replay` |
| B10 | Rule 5: `src/` never imports `experiments/` | `[MUST]` | `30 § rule 5`; `docs/experiments/README § Rule` | contract `src never imports experiments` | B | **SATISFIED** — contract 'rule 5'; `experiments/` not created (unowned) |
| B11 | `runtime/` is the only module that reads a clock; `SystemClock` (FULL) and `LogClock` (REPLAY) are injected via `Clock.now()` | `[DEMO-CRITICAL]` | `19 § Clocks`; `22 § Clock`; `30 § rule 6` | `src/runtime/clock.py`; A's AST lint green on B's packages | B | **SATISFIED** — `src/runtime/clock.py:22 SystemClock`, `:29 LogClock`; `tests/property/test_architecture.py::test_no_clock_in_core` green on all core packages; p0 plant (b) |
| B12 | A meta-test asserts the import-linter contract set is non-empty and `lint-imports` is wired into the suite (it is a test, not a convention) | `[MUST]` | `39 § 7 Enforcing the architecture mechanically` | `tests/integration/meta/test_layer_dag.py` | B | **SATISFIED** — `tests/integration/meta/test_layer_dag.py::test_contract_set_covers_every_rule_in_30`, `::test_lint_imports_passes_on_the_real_tree` |
| B13 | Repository tree matches `30` path-for-path; no directory `30` does not list; `.gitignore` per `40` § Prerequisite with the `!data/build/` `!data/eval/` amendment | `[MUST]` | `30 § tree`; `40 § Prerequisite`; BUILD_B §1.4 collision 3 | `find -type d` vs the tree; `.gitignore` content | B | **SATISFIED** — tree matches `30` (`find -type d`); `.gitignore` = `data/*` + `!data/build/` + `!data/eval/` (+ `.venv/`, `*.egg-info/`, `.claude/`) |
| B14 | Config hierarchy `defaults.yaml <- profiles/<p>.yaml <- station/<id>.yaml <- env overrides (dev only)`; later layers override earlier | `[DEMO-CRITICAL]` | `31 § Hierarchy` | `tests/integration/runtime/test_config.py::test_hierarchy` | B | **SATISFIED** — `src/runtime/config.py::load` `_deep_merge` chain; `test_config_rejects_malformed.py::test_hierarchy_station_overrides_defaults` |
| B15 | `prod` and `demo` profiles **reject** env overrides | `[DEMO-CRITICAL]` | `31 § Hierarchy` | `test_config.py::test_demo_rejects_env_overrides` | B | **SATISFIED** — `test_demo_and_prod_reject_env_overrides_but_dev_applies_them` |
| B16 | Startup validation before `FULL`: schema of every layer; every `zone.contents` ingredient resolves in the knowledge bundle; every `menu_item.required_zones` exists in the station map; range checks on all time windows; checksum of station + knowledge bundles | `[DEMO-CRITICAL]` | `31 § Validation`; `23 § P6, P12`; `ADR-0007` | `test_config.py::test_unresolvable_zone_contents`, `::test_missing_required_zone`, `::test_window_range`, `::test_checksum` | B | **SATISFIED** — `config.py::load` — pydantic `Strict` models per layer, referential checks (zone contents, menu zones/ingredients, taxonomy parents), `Field` range bounds on every window, `_checksum` over station + knowledge files; 15 parametrized plants |
| B17 | Any validation failure → runtime remains in `CALIBRATION` with a specific error and refuses `FULL`; safety-critical config never loads partially | `[DEMO-CRITICAL]` | `31 § Validation`; `23 § P12` | `tests/integration/runtime/test_controller.py::test_bad_config_stays_in_calibration` | B | **SATISFIED** — `src/runtime/controller.py:49 request_mode` refuses FULL without `report_config(True)`; `test_controller.py::test_bad_config_stays_in_calibration_and_refuses_full`; `app.py::build` transitions only after a successful `load` |
| B18 | `config_version` + `knowledge_version` emitted in `CONFIG_LOADED` at the head of every session log | `[DEMO-CRITICAL]` | `31 § Versioning`; `12 § System events`; `18 § Versioning` | first committed event of a runtime session is `CONFIG_LOADED` | B | **SATISFIED** — `src/runtime/app.py:135` emits CONFIG_LOADED in `Runtime.__init__`; `test_app.py::test_starts_in_protocol_only_with_config_loaded_first` (first committed event) |
| B19 | Zone authoring rule applied: `INGREDIENT` polygons generous, `CLEAN_STOCK` polygons conservative, documented in the station file | `[MUST]` | `31 § Zone authoring rule`; `20 § Known spatial limitations` | inspection of `config/station/demo.yaml` | B | **SATISFIED** — `config/station/demo.yaml` header + polygons (INGREDIENT +30 mm, CLEAN_STOCK −20 mm) |
| B20 | Knowledge bundle: taxonomy graph with Big-9 roots; `PINE_NUT` under `TREE_NUT` by default (conservative); ~25 ingredients with `contains`/`may_contain`, aliases, `source`; ~8 menu items with `required_zones`; `knowledge_version` | `[DEMO-CRITICAL]` | `18 § Three layers, § Versioning`; `35 § What each layer needs`; `decisions/README § Pending` | `config/knowledge/demo/` loads through A's provider and B's validator | B | **SATISFIED** — `config/knowledge/demo/{taxonomy,ingredients,menu_items}.yaml` (19/28/8, PINE_NUT→TREE_NUT); `tests/unit/knowledge/test_provider.py` |
| B21 | Demo station config: bins `pesto`/`mayo`/`turkey`/`bread` (each a zone and a container carrier), carriers `gloves`/`spreader`/`board`/`landing`, zones of every kind incl. `GLOVE_DISPENSER`, `CLEAN_STOCK`, `TOOL_RACK`, `WORK`, `LANDING`, `WASH`; polygons in station-frame mm; `config_version`; profiles `dev/demo/eval/prod` | `[DEMO-CRITICAL]` | `37 § Physical setup`; `03 § header`; `20 § Zones`; `11 § Zone, Relationships`; `31 § Hierarchy` | `config/station/demo.yaml` + `config/profiles/*.yaml` validate | B | **SATISFIED** — `config/station/demo.yaml`, `config/profiles/{dev,demo,eval,prod}.yaml`; `test_config_to_domain.py` |
| B22 | Every tunable lives in config, not code: all `19` windows (`t_dwell` 250ms, `t_hysteresis` 400ms, `t_reorder` 500ms, `t_stale[kind]` 20/120/300/600s, `t_occlusion_max` 3s, `station_recent_window` 30m, `t_escalate` 20s, `t_cooldown` 120s, `t_abandon` 15m), `max_hops` 3, grade thresholds per event type, `frame_buffer_seconds` 3, `audit_days` 7 | `[DEMO-CRITICAL]` | `31 § Principle`; `19 § Windows`; `14 § Parameters`; `24 § Data lifecycle` | keys present in `config/defaults.yaml` | B | **SATISFIED** — `config/defaults.yaml`; `config.py:433 to_domain` maps every key onto `domain.Config` |
| B23 | `RuntimeController.mode()`, `request_mode(m, cause)` emits `STATION_MODE_CHANGED`, `health()`; it is the **only** owner of mode transitions; the only feedback edges into the log are worker assertion and mode change | `[DEMO-CRITICAL]` | `22 § RuntimeController`; `10 § Feedback edges, § Runtime modes` | `tests/integration/runtime/test_controller.py` | B | **SATISFIED** — `controller.py:46 mode`, `:49 request_mode` (emits STATION_MODE_CHANGED via the sink), `:68 health`; `test_controller.py` (5 tests); the only other feedback edge is `on_action` (worker events) |
| B24 | Composition root runs the `39` §1 loop in **one process**: drain queue → reorder → append log → reduce → risk → policy → broadcast state → serve UI → accept worker actions; event log persisted as JSONL, one file per session | `[DEMO-CRITICAL]` | `39 § 1, § 5`; `ADR-0012` | `tests/integration/runtime/test_loop.py` | B | **SATISFIED** — `app.py:267 tick` (reorder pump) → `EventLog.append` → `_on_committed` → `reduce` → `:203 _assess` → `evaluate` → `/ws` broadcast; JSONL per session in `Runtime.__init__`; `test_app.py::test_build_boots_the_real_thing` |
| B25 | Risk + policy evaluation is synchronous with `TICKET_BOUND` so Tier 0 fires **before any motion** | `[DEMO-CRITICAL]` | `37 § Architectural requirements 3`; `15 § Precondition check` | `test_loop.py::test_tier0_fires_at_bind` | B | **SATISFIED** — `_on_committed` runs `_assess` synchronously on every mutating event; `test_app.py::test_restricted_bind_fires_tier0_before_any_motion_and_resolves_in_one_tap`; live samples (`p3_fallback_ladder.json` +0:09.5) |
| B26 | Reducer / risk-engine exception at the runtime boundary → `PROTOCOL_ONLY`, existing Tier 2 holds persist, incident logged with the triggering `event_id`; never fail open to silence | `[DEMO-CRITICAL]` | `23 § P11`; `02 § Fail-safe`; `ADR-0011` | `test_controller.py::test_core_exception_enters_protocol_only` | B | **SATISFIED** — `_on_committed` except → `controller.report_core_exception`; `test_controller.py::test_core_exception_enters_protocol_only_and_never_fails_open`; state/alerts untouched by the failing event so holds persist |
| B27 | A malformed event rejected at `append` never crashes the runtime; `HEALTH_DEGRADED` surfaced; quarantined for offline diagnosis | `[MUST]` | `23 § P10`; `22 § EventSink, EventLog` | `test_loop.py::test_malformed_event_does_not_crash` | B | **SATISFIED** — `src/events/log.py` `EventSink.emit` never raises, quarantines + HEALTH_DEGRADED; `tests/replay/test_runner.py` quarantine case; `test_app.py::test_malformed_action_never_crashes` |
| B28 | `PROTOCOL_ONLY` complete: enterable on health degradation, core exception, or operator request; Tier 0 on every restricted bind; Tier 1 disabled; Tier 2 holds persist; UI states plainly that vision is unavailable; recovery to `FULL` restores no carrier trust | `[DEMO-CRITICAL]` | `10 § Runtime modes`; `ADR-0011`; `38 § P3 scope`; `23 § P5` | `test_loop.py::test_protocol_only_*`; mode banner rendered | B | **SATISFIED** — `app.py::build` → PROTOCOL_ONLY; reducer sets all UNKNOWN (`tests/unit/state/test_system.py`); policy demotes Tier 1 in PROTOCOL_ONLY (`tests/unit/policy/test_evaluate.py`); scenario L; banner text; `p3_protocol_only_rehearsal.json` |
| B29 | `REPLAY` mode: layers 1–4 replaced by a log/scenario reader; layers 5–12 byte-identical; `LogClock` driven by `t_occurred`; nothing reads a wall clock | `[DEMO-CRITICAL]` | `10 § Runtime modes`; `22 § Clock`; `36 § Why replay is foundational` | replay of `scenarios/demo.yaml` and of a recorded JSONL produce identical snapshots | B | **SATISFIED** — `src/runtime/feed.py` + `LogClock`; `tests/integration/runtime/test_replay_mode.py` (runtime == ReplayRunner; recorded log == fixture); `p3_fallback_ladder.json` |
| B30 | `CALIBRATION` mode: no tickets may bind | `[MUST]` | `10 § Runtime modes` | `test_controller.py::test_calibration_blocks_bind` | B | **SATISFIED** — reducer refuses TICKET_BOUND in CALIBRATION (`tests/unit/state/test_tickets.py`); `controller.py:110 bind_allowed`; `test_controller.py::test_bad_config_stays_in_calibration_and_refuses_full` |
| B31 | Runtime wires `FixtureSource` (replay/demo) and `ManualEntrySource` (display) as the `OrderSource`s; `TICKET_BOUND` is only ever operator-sourced (a tap), never inferred | `[DEMO-CRITICAL]` | `22 § OrderSource`; `17 § Binding`; `ADR-0006` | bind reachable only via `POST /action` | B | **SATISFIED** — `app.py::tick` polls `ManualEntrySource`; `on_action('BIND_TICKET')` is the only path to TICKET_BOUND (source OPERATOR, enforced by the catalog validator) |
| B32 | No network dependency anywhere in the safety core or `PROTOCOL_ONLY`; single-host, loopback-only; no egress path in `src/` | `[DEMO-CRITICAL]` | `24 § The line that is never crossed, § Threat model, § out of scope` | server binds `127.0.0.1`; grep `src/` for outbound HTTP | B | **SATISFIED** — `config/defaults.yaml runtime.host: 127.0.0.1`; grep `requests|urllib|httpx` in `src/` = 0 |
| B33 | FastAPI + WebSocket + a single vanilla HTML/CSS/JS page; no build step; **full** state pushed at 5Hz | `[DEMO-CRITICAL]` | `39 § 6` | `src/ui/server.py`; `tests/integration/ui/test_ws.py` | B | **SATISFIED** — `src/ui/server.py:28 create_app`, `:51 /ws` loop at `push_hz` (5 from config); `tests/integration/ui/test_server.py` |
| B34 | `POST /action` → a worker event (`OPERATOR_ASSERTION` et al.) in the log; the UI **never** mutates `WorldState` directly | `[DEMO-CRITICAL]` | `22 § WorkerDisplay`; `10 § Feedback edges`; `26 § Worker actions` | `tests/integration/runtime/test_app.py` (every action kind becomes an event); grep `src/ui` for state mutation | B | **SATISFIED** — `server.py:45 POST /action` → `app.py:300 on_action` emits drafts only; no state write anywhere in `src/ui` (audit grep) |
| B35 | Routes: `/` worker display; `/inspector` read-only state + event timeline; inspector has no write route and reads the same projections as the runtime | `[DEMO-CRITICAL]` | `39 § 6`; `25 § Inspector` | HTTP 200 on both; route table has one `POST` | B | **SATISFIED** — `server.py:32,36` routes; `test_server.py::test_routes_and_no_write_but_action`; live inspector has 0 buttons |
| B36 | Wire shape is A's `DisplayPayload` (`state_summary` + `interventions`, generated from `src/domain`) plus a runtime block; the mock at `src/ui/static/mock/snapshot.example.json` validates against `src/ui/wire.py`; `data/build/snapshot.{example,schema}.json` are kept identical to it | `[DEMO-CRITICAL]` | `22 § WorkerDisplay`; BUILD_B §1.5 | `tests/integration/ui/test_snapshot_shape.py` validates mock + live payload against the schema | B | **SATISFIED** — adopted A's `DisplayPayload` verbatim as the core of `src/ui/wire.py:80 Snapshot` + runtime block; `tests/integration/ui/test_snapshot_shape.py`; live payload validated (`test_app.py::test_snapshot_validates_against_the_wire_shape`) |
| B37 | Tier 0 reset-prompt surface: checklist at `TICKET_BOUND`; items tick themselves off as resets are observed or asserted; auto-resolves when preconditions met; reads as help, not interrogation | `[DEMO-CRITICAL]` | `26 § Tier 0`; `16 § Lifecycle` | renders from mock; live in beats 3→4 | B | **SATISFIED** — `app.js renderSurface` tier 0; live: items tick at +0:12.6/13.6/14.6 and clear at +0:16.7 (`p3_fallback_ladder.json`) |
| B38 | Tier 1 interrupt surface: single line, dismissible, names the specific carrier and action, `[Swap tool] [Already swapped]`; shows its own resolution rather than silently clearing | `[DEMO-CRITICAL]` | `26 § Tier 1`; `16 § Lifecycle` | renders from mock | B | **SATISFIED** — `app.js` tier 1 branch (`Swap tool`/`Already swapped`/`Dismiss`); live: +0:22.6 'STOP — spreader_2 touched pesto' |
| B39 | Tier 2 hold surface: the only blocking surface; does not auto-clear; shows the evidence trace and the disclaimer *"This is an observation, not a determination. Confirm with the cook."*; `[Cook confirms tool was clean] [Remake]` | `[DEMO-CRITICAL]` | `26 § Tier 2`; `37 § beat 5`; `16 § Rules` | renders from mock | B | **SATISFIED** — `app.js` tier 2 branch: trace + exact disclaimer + `Cook confirms … was clean` + `Remake`; no Dismiss; `on_action` refuses DISMISS for tier 2; live +0:25.7 |
| B40 | Worker actions: Acknowledge, "Already swapped"/"Already clean", Dismiss (Tier 0/1 only), Remake, Resolve hold — each enters the log as an event; dismissal suppresses the display, never state | `[DEMO-CRITICAL]` | `26 § Worker actions, § What dismissal does`; `02 § Human responsibility` | `tests/integration/runtime/test_app.py` one case per action kind; `tests/unit/policy/test_state.py` for dismissal/expiry | B | **SATISFIED** — `app.py:300 on_action` map (CG-B5); `test_app.py` (assert, bind, prep, complete, release, refusals); dismissal = ALERT_UPDATED with worker_slot → `dismissed_until` only (`tests/unit/policy/test_state.py`) |
| B41 | Every alert type resolvable in ≤ 2 taps, **measured on a teammate, not the author** | `[DEMO-CRITICAL]` | `38 § P3 Gate`; `26 § constraint 3`; `34 § Secondary — UX` | measurement record (who, tier, taps) in `status_B.md` | B | **PARTIAL** — mechanically ≤ 2: Tier 0 = 1 (`All done — station reset`), Tier 1 = 1 (`Already swapped`), Tier 2 = 2 (`Cook confirms` + `Release ticket`) or 1 (`Remake`) — `p3_protocol_only_rehearsal.json`; **not yet measured on a teammate** (driver cannot) |
| B42 | Glanceable in < 1s: headline ≤ 6 words rendered large; required actions as icon + noun; no dashboard on the worker surface; silence is the default state | `[DEMO-CRITICAL]` | `26 § Design constraints 1, 2, 4` | inspection of `/` with no alerts and with each tier | B | **SATISFIED** — `domain.Alert` headline validator (≤ 6 words); `.headline` clamp(26–44px); icons + nouns; default surface 'Station quiet / Nothing to do' |
| B43 | Evidence-trace rendering: one line per `TraceStep` (time, type, participants, grade, delta) **plus absence lines**; rendered from the same derivation object as the alert | `[DEMO-CRITICAL]` | `25 § The evidence trace`; `38 § P3 scope` | trace pane renders the mock Tier 2 trace incl. the `-- no GLOVE_CHANGE observed --` line | B | **SATISFIED** — `app.js traceLines` (time, rule, narrative, grade; `.trace-line.absent`); same `derivation` in the alerts pane and the Tier 2 surface; live: 5 lines incl. 1 absence |
| B44 | Live carrier grid: one tile per carrier showing **both axes** (taint set + epistemic status); tiles go amber as taint spreads; built in P3, not P6 | `[DEMO-CRITICAL]` | `37 § req 1`; `39 § 6`; `13 § Two independent axes` | beats 1–2 in replay | B | **SATISFIED** — `app.js tile()` (taint chips + epistemic + `data-tainted` amber); live: amber spreads gloves→spreader→board→bin:mayo and clears on resets |
| B45 | **No green state anywhere** in the UI; no prohibited claim words ("safe", "contaminated", "clean because wiped", "allergen-free") in any UI copy B authors | `[DEMO-CRITICAL]` | `02 § Unsupported claims`; `40 § Session 3 prompt`; `39 § 6.2` | grep CSS for green hues; `tests/integration/ui/test_copy_lint.py` | B | **SATISFIED** — `tests/integration/ui/test_copy_lint.py` (no green hue, no claim word); `domain.PROHIBITED_CLAIMS` validators; `tests/property::test_alert_copy_guard` over `src/policy/copy.py` |
| B46 | Never accusatory; no per-person feedback; no worker or customer identity rendered anywhere — the only worker field is `worker_slot` | `[DEMO-CRITICAL]` | `26 § constraints 5–6`; `24 § Structural guarantees`; `ADR-0010` | grep UI for name/id fields; copy review | B | **SATISFIED** — `test_snapshot_shape.py::test_exactly_three_epistemic_values_and_no_safe_member_or_identity`; copy lint ACCUSATORY/IDENTITY regexes; only `worker_slot` on the wire |
| B47 | Mode banner: `PROTOCOL_ONLY` states plainly vision is unavailable; reasoning-down state visibly stated; `CALIBRATION` shows the specific config error | `[DEMO-CRITICAL]` | `10 § Runtime modes`; `23 § P5, P11, P12`; `37 § req 5` | render with each mode | B | **SATISFIED** — `app.js renderTop` banner: PROTOCOL_ONLY text, DOWN state with `health.message`, CALIBRATION with message, REPLAY label — mock checks (down/quiet cases) |
| B48 | Inspector shows carrier states on both axes, zones + recent contacts, bound tickets + restrictions, active alerts with traces, mode + health (incl. rejected events and late rate), and a scrubbable event timeline | `[MUST]` | `25 § Inspector, § Log levels`; `19 § Ordering` | `/inspector` renders all panes from the live socket | B | **SATISFIED** — `inspector.html` + `app.js renderInspector`: carriers both axes, zones + last contact, tickets, alerts + traces, health (late rate, rejected, incident), scrubbable timeline — live check seq 25 |
| B49 | Aesthetic: industrial HMI — radius ≤ 3px; no box-shadows on data surfaces (1px borders); no gradients; monospace `tabular-nums` for every number/id; state as thick left borders + fills, not pills; uppercase wide-tracked micro-labels; dark ground that ignores viewer theme; **no charts**; four panes in dataflow order, worker surface at the bottom | `[DEMO-CRITICAL]` | `39 § 6.1, § 6.2`; `40 § Session 3 prompt` | CSS grep for `border-radius` > 3px, `box-shadow`, `gradient`; visual check | B | **SATISFIED** — `style.css` (`--r: 2px`, no box-shadow, no gradient, `tabular-nums`, `--band` left borders, `.micro`, `color-scheme: dark`); `test_copy_lint.py::test_hmi_rules_radius_shadow_gradient_charts`; live audit radii {0px, 2px} |
| B50 | Every UI asset vendored into `src/ui/static/` (Open Props, Phosphor SVG sprite, Archivo + IBM Plex Sans/Mono woff2 with real fallback stacks); no CDN, no component library, no Tailwind, no bundler, no Lucide; page loads with networking disabled | `[DEMO-CRITICAL]` | `39 § 6.1`; `24 § The line that is never crossed` | grep templates for `http`; offline load | B | **SATISFIED** — `src/ui/static/vendor/` (open-props 24 KB, 27-symbol Phosphor sprite, 9 woff2 + `fonts.css` with fallbacks); `test_no_runtime_network_dependency_or_forbidden_toolkit`; `scripts/vendor_ui_assets.sh` fails on empty files |
| B51 | Presentation-layer suppression: one visible alert per station, highest tier wins | `[DEMO-CRITICAL]` | `16 § Alert identity — the anti-spam mechanism` (table row "Presentation") | render mock with several alerts → one primary surface | B | **SATISFIED** — `app.js surfaceFor()` highest tier wins, dismissed hidden; mock check 'dismissed' |
| B52 | Beat 0: four carriers shown, system silent | `[DEMO-CRITICAL]` | `37 § Script beat 0` | run book step 0 | B | **SATISFIED** — live start: grid of 8 tiles, surface quiet (`p3_fallback_ladder.json`) |
| B53 | Beat 1: bind ticket 47 (no restriction) → **silence**; `gloves`, `spreader`, `board` turn amber | `[DEMO-CRITICAL]` | `37 § beat 1`; `03 § A` | replay of `scenarios/demo.yaml` (A) through B's UI | B | **SATISFIED** — live +0:07.5: amber gloves/spreader/board, no alert; `scenarios/demo.yaml` `no_alerts_of_tier {0, before 9000}` |
| B54 | Beat 2: reach into mayo → `bin:mayo` flips amber: *"Shared container — PINE_NUT — affects all future tickets"*; emerges as a plain pathway, not a special case | `[DEMO-CRITICAL]` | `37 § beat 2, § req 2`; `03 § I`; `14 § Pathway classes` | replay | B | **SATISFIED** — live: bin:mayo amber with 'Shared container — … — affects all future tickets'; `scenarios/I-back-contamination.yaml`; `tests/unit/risk/test_pathways.py` back-contamination is an ordinary pathway |
| B55 | Beat 3: bind ticket 48 (PINE_NUT) → Tier 0 **before any motion** listing gloves / spreader / use sealed backup mayo | `[DEMO-CRITICAL]` | `37 § beat 3, § req 3` | replay | B | **SATISFIED** — live +0:09.5: Tier 0 at bind listing gloves / spreader / mayo (use sealed backup) / board |
| B56 | Beat 4: glove change + spreader swap → checklist ticks itself, prompt clears, prep proceeds silently | `[DEMO-CRITICAL]` | `37 § beat 4, § req 4` | replay | B | **SATISFIED** — live +0:12.6→16.7: items tick, prompt clears, silence |
| B57 | Beat 5: rerun 48 ignoring the prompt → **Tier 2 HOLD** with the full evidence trace and the disclaimer line | `[DEMO-CRITICAL]` | `37 § beat 5` | replay | B | **SATISFIED** — live +0:25.7: HOLD with trace and disclaimer; T49 HELD |
| B58 | Beat 6 close; beats 3 and 6 survive with zero perception (`PROTOCOL_ONLY` presentable) | `[DEMO-CRITICAL]` | `37 § beat 6, § req 5` | `PROTOCOL_ONLY` run | B | **SATISFIED** — `p3_protocol_only_rehearsal.json`: bind + Tier 0 + resolve + hold path with zero perception |
| B59 | Fallback ladder: live perception → recorded log replay → `scenarios/demo.yaml`, **all producing identical UI**; camera-unplug drill rehearsed for real; run book written beat by beat | `[DEMO-CRITICAL]` | `37 § Determinism under pressure`; `38 § P6 gate` | rehearsal record in `status_B.md`; `data/build/runbook_B.md` | B | **PARTIAL** — levels 2 and 3 identical on the display and in `test_replay_mode.py`; level 1 N/A (no camera); unplug drill rehearsed as scenario L replay; `data/build/runbook_B.md` |
| B60 | **A complete demo runs in `PROTOCOL_ONLY` with no camera attached** (the P3 gate) | `[DEMO-CRITICAL]` | `38 § P3 Gate`; `39 § 9 The cut line` | run book executed end to end; `P3 GATE GREEN` posted | B | **SATISFIED** — `p3_protocol_only_rehearsal.json`, `test_app.py`; gate clause 3 of 3 for P3 (clause 1 is B41 PARTIAL, clause 2 is B61) |
| B61 | Alert-copy lint green on B's copy: if A's lint fails on B-authored strings, fix the copy, never the lint | `[DEMO-CRITICAL]` | `38 § P3 Gate`; `02 § Unsupported claims`; `33 § Required negative tests` | A's lint + B's `test_copy_lint.py` | B | **SATISFIED** — `tests/property/test_architecture.py::test_alert_copy_guard` + `tests/integration/ui/test_copy_lint.py` green |
| B62 | `PerceptionPipeline.start/stop/health` writes only to `EventSink`; honesty obligations (`CARRIER_OBSERVABILITY_CHANGED`, `TRACK_IDENTITY_SUSPECT`, `HEALTH_DEGRADED`); occlusion-reporting recall ≥ 0.95; NR < 1.0/hr; contact-to-alert < 1.5s; frames never persisted by default | `[SHOULD]` | `22 § PerceptionPipeline`; `21 § Obligations of honesty`; `38 § P4/P5 gates`; `34 § Secondary`; `24 § guarantee 4` | **OPEN — no hardware** (`status_B.md`) | B | **MISSING** — OPEN by design — no hardware (`status_B.md` §Hardware check); `src/perception/` empty; nothing camera-dependent built |
| B63 | `[DEFER]` authentication, TLS, RBAC, multi-tenancy, network transport — **not built** | `[DEFER]` | `38 § Explicitly defer`; `24 § Deliberately out of scope` | grep `src/` for `auth`, `jwt`, `login`, `ssl` → none | B | **SATISFIED** — grep `auth|jwt|login|ssl|tls|rbac|tenant` in `src/` = 0 |
| B64 | `[DEFER]` mobile app, analytics, per-person metrics, manager surface, multi-station aggregation — **not built** | `[DEFER]` | `38 § Explicitly defer, § Could build`; `26 § constraint 6, § Escalation` | grep; route table | B | **SATISFIED** — grep `analytics|mobile|manager_surface` = 0; no per-person metric anywhere; route table = `/`, `/inspector`, `/snapshot`, `/action`, `/ws` |
| B65 | `[DEFER]` POS/delivery adapters; evidence video clips / frame persistence; rag/wipe-cloth perception; wash-cycle detection; ML risk model — **not built** | `[DEFER]` | `38 § Explicitly defer`; `24 § A deliberate rejection`; `14 § Rag modeling` | grep `src/` for `<video`, `persist_frames`, `cv2` outside `perception/`; `orders/adapters/` holds only A's sources | B | **SATISFIED** — grep `cv2|video|persist_frames(feature)|pos|doordash|wash_cycle_detect|rag` = 0 (only the loader guard forbidding `persist_frames`); `src/orders/adapters/` = manual + fixture only |
