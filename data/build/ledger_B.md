# Requirements Ledger — Device B (Shell & Surface)

Rows `B1..Bn`. Concatenates with A's `A1..An` at convergence. Priority tags per BUILD_B §2.2.
Status column filled at convergence: `SATISFIED` / `PARTIAL` / `MISSING`, with file:line evidence.

Scope (from the §2.4 hardware check, 2026-09-12 06:36 EDT): **P0 harness clauses, P3 in full, all content. P4–P6 OPEN — no camera, no fixtures.**

| id | requirement (testable condition) | priority | source (`file § heading`) | verified_by | owner | status |
|---|---|---|---|---|---|---|
| B1 | `import-linter` rejects a deliberately planted violation (a `state/` module importing `perception/`) and exits 0 without it | `[DEMO-CRITICAL]` | `38-workflow.md § P0 — Gate`; `39 § 7` | `lint-imports` exit≠0 with plant; `tests/integration/meta/test_layer_dag.py` | B | |
| B2 | Config validation rejects a deliberately malformed bundle with a specific error — never a silent default, never a partial load | `[DEMO-CRITICAL]` | `38 § P0 — Gate`; `31 § Validation`; `23 § P12` | `tests/integration/meta/test_config_rejects_malformed.py` | B | |
| B3 | `pip install -e .` installs core + runtime only; `perception` and `dev` are extras | `[DEMO-CRITICAL]` | `39 § 2 Dependencies` | `uv pip install -e .` then `import cv2` fails | B | |
| B4 | Safety core importable, runnable, testable with `opencv` absent; nothing under `runtime/` imports `perception/` at module scope (lazy import inside the composition root only) | `[DEMO-CRITICAL]` | `39 § 2`; `32 § Principles 1–2`; `ADR-0001` | `pytest -q tests/integration` in env without cv2; grep `src/runtime` for top-level `src.perception` | B | |
| B5 | Dependency set is exactly `39` §2 (pydantic ~2.9, pyyaml ~6.0, fastapi ~0.115, uvicorn ~0.32; extras opencv ~4.10, numpy ~2.1, mediapipe ~0.10; dev pytest, pytest-asyncio, hypothesis ~6, import-linter ~2.0, mypy ~1.11, ruff ~0.7). Nothing else; core-group additions need an ADR | `[DEMO-CRITICAL]` | `39 § 2`; `32 § Selection gate` | diff `pyproject.toml` against the table | B | |
| B6 | Rule 1: `domain/` imports nothing internal | `[DEMO-CRITICAL]` | `30 § Dependency rules 1`; `22 § Dependency direction` | contract `domain imports nothing internal` | B | |
| B7 | Rule 2: `perception/` imports **only** `domain/` and `events/` | `[DEMO-CRITICAL]` | `30 § rule 2`; `21 § Non-obligations`; `22 § PerceptionPipeline` | contract `perception imports only domain and events` | B | |
| B8 | Rule 3: `state/ risk/ policy/ orders/` never import `perception/ ui/ runtime/` | `[DEMO-CRITICAL]` | `30 § rule 3` | contract `core never imports the shell` | B | |
| B9 | Rule 4: `runtime/` is the only composition root — no other package imports `runtime/`; only `runtime/` imports `ui/`; only `runtime/`/`replay/` reach across layers | `[DEMO-CRITICAL]` | `30 § rule 4`; `22 § Dependency direction` | contract `runtime is the only composition root` | B | |
| B10 | Rule 5: `src/` never imports `experiments/` | `[MUST]` | `30 § rule 5`; `docs/experiments/README § Rule` | contract `src never imports experiments` | B | |
| B11 | `runtime/` is the only module that reads a clock; `SystemClock` (FULL) and `LogClock` (REPLAY) are injected via `Clock.now()` | `[DEMO-CRITICAL]` | `19 § Clocks`; `22 § Clock`; `30 § rule 6` | `src/runtime/clock.py`; A's AST lint green on B's packages | B | |
| B12 | A meta-test asserts the import-linter contract set is non-empty and `lint-imports` is wired into the suite (it is a test, not a convention) | `[MUST]` | `39 § 7 Enforcing the architecture mechanically` | `tests/integration/meta/test_layer_dag.py` | B | |
| B13 | Repository tree matches `30` path-for-path; no directory `30` does not list; `.gitignore` per `40` § Prerequisite with the `!data/build/` `!data/eval/` amendment | `[MUST]` | `30 § tree`; `40 § Prerequisite`; BUILD_B §1.4 collision 3 | `find -type d` vs the tree; `.gitignore` content | B | |
| B14 | Config hierarchy `defaults.yaml <- profiles/<p>.yaml <- station/<id>.yaml <- env overrides (dev only)`; later layers override earlier | `[DEMO-CRITICAL]` | `31 § Hierarchy` | `tests/integration/runtime/test_config.py::test_hierarchy` | B | |
| B15 | `prod` and `demo` profiles **reject** env overrides | `[DEMO-CRITICAL]` | `31 § Hierarchy` | `test_config.py::test_demo_rejects_env_overrides` | B | |
| B16 | Startup validation before `FULL`: schema of every layer; every `zone.contents` ingredient resolves in the knowledge bundle; every `menu_item.required_zones` exists in the station map; range checks on all time windows; checksum of station + knowledge bundles | `[DEMO-CRITICAL]` | `31 § Validation`; `23 § P6, P12`; `ADR-0007` | `test_config.py::test_unresolvable_zone_contents`, `::test_missing_required_zone`, `::test_window_range`, `::test_checksum` | B | |
| B17 | Any validation failure → runtime remains in `CALIBRATION` with a specific error and refuses `FULL`; safety-critical config never loads partially | `[DEMO-CRITICAL]` | `31 § Validation`; `23 § P12` | `tests/integration/runtime/test_controller.py::test_bad_config_stays_in_calibration` | B | |
| B18 | `config_version` + `knowledge_version` emitted in `CONFIG_LOADED` at the head of every session log | `[DEMO-CRITICAL]` | `31 § Versioning`; `12 § System events`; `18 § Versioning` | first committed event of a runtime session is `CONFIG_LOADED` | B | |
| B19 | Zone authoring rule applied: `INGREDIENT` polygons generous, `CLEAN_STOCK` polygons conservative, documented in the station file | `[MUST]` | `31 § Zone authoring rule`; `20 § Known spatial limitations` | inspection of `config/station/demo.yaml` | B | |
| B20 | Knowledge bundle: taxonomy graph with Big-9 roots; `PINE_NUT` under `TREE_NUT` by default (conservative); ~25 ingredients with `contains`/`may_contain`, aliases, `source`; ~8 menu items with `required_zones`; `knowledge_version` | `[DEMO-CRITICAL]` | `18 § Three layers, § Versioning`; `35 § What each layer needs`; `decisions/README § Pending` | `config/knowledge/demo/` loads through A's provider and B's validator | B | |
| B21 | Demo station config: bins `pesto`/`mayo`/`turkey`/`bread` (each a zone and a container carrier), carriers `gloves`/`spreader`/`board`/`landing`, zones of every kind incl. `GLOVE_DISPENSER`, `CLEAN_STOCK`, `TOOL_RACK`, `WORK`, `LANDING`, `WASH`; polygons in station-frame mm; `config_version`; profiles `dev/demo/eval/prod` | `[DEMO-CRITICAL]` | `37 § Physical setup`; `03 § header`; `20 § Zones`; `11 § Zone, Relationships`; `31 § Hierarchy` | `config/station/demo.yaml` + `config/profiles/*.yaml` validate | B | |
| B22 | Every tunable lives in config, not code: all `19` windows (`t_dwell` 250ms, `t_hysteresis` 400ms, `t_reorder` 500ms, `t_stale[kind]` 20/120/300/600s, `t_occlusion_max` 3s, `station_recent_window` 30m, `t_escalate` 20s, `t_cooldown` 120s, `t_abandon` 15m), `max_hops` 3, grade thresholds per event type, `frame_buffer_seconds` 3, `audit_days` 7 | `[DEMO-CRITICAL]` | `31 § Principle`; `19 § Windows`; `14 § Parameters`; `24 § Data lifecycle` | keys present in `config/defaults.yaml` | B | |
| B23 | `RuntimeController.mode()`, `request_mode(m, cause)` emits `STATION_MODE_CHANGED`, `health()`; it is the **only** owner of mode transitions; the only feedback edges into the log are worker assertion and mode change | `[DEMO-CRITICAL]` | `22 § RuntimeController`; `10 § Feedback edges, § Runtime modes` | `tests/integration/runtime/test_controller.py` | B | |
| B24 | Composition root runs the `39` §1 loop in **one process**: drain queue → reorder → append log → reduce → risk → policy → broadcast state → serve UI → accept worker actions; event log persisted as JSONL, one file per session | `[DEMO-CRITICAL]` | `39 § 1, § 5`; `ADR-0012` | `tests/integration/runtime/test_loop.py` | B | |
| B25 | Risk + policy evaluation is synchronous with `TICKET_BOUND` so Tier 0 fires **before any motion** | `[DEMO-CRITICAL]` | `37 § Architectural requirements 3`; `15 § Precondition check` | `test_loop.py::test_tier0_fires_at_bind` | B | |
| B26 | Reducer / risk-engine exception at the runtime boundary → `PROTOCOL_ONLY`, existing Tier 2 holds persist, incident logged with the triggering `event_id`; never fail open to silence | `[DEMO-CRITICAL]` | `23 § P11`; `02 § Fail-safe`; `ADR-0011` | `test_controller.py::test_core_exception_enters_protocol_only` | B | |
| B27 | A malformed event rejected at `append` never crashes the runtime; `HEALTH_DEGRADED` surfaced; quarantined for offline diagnosis | `[MUST]` | `23 § P10`; `22 § EventSink, EventLog` | `test_loop.py::test_malformed_event_does_not_crash` | B | |
| B28 | `PROTOCOL_ONLY` complete: enterable on health degradation, core exception, or operator request; Tier 0 on every restricted bind; Tier 1 disabled; Tier 2 holds persist; UI states plainly that vision is unavailable; recovery to `FULL` restores no carrier trust | `[DEMO-CRITICAL]` | `10 § Runtime modes`; `ADR-0011`; `38 § P3 scope`; `23 § P5` | `test_loop.py::test_protocol_only_*`; mode banner rendered | B | |
| B29 | `REPLAY` mode: layers 1–4 replaced by a log/scenario reader; layers 5–12 byte-identical; `LogClock` driven by `t_occurred`; nothing reads a wall clock | `[DEMO-CRITICAL]` | `10 § Runtime modes`; `22 § Clock`; `36 § Why replay is foundational` | replay of `scenarios/demo.yaml` and of a recorded JSONL produce identical snapshots | B | |
| B30 | `CALIBRATION` mode: no tickets may bind | `[MUST]` | `10 § Runtime modes` | `test_controller.py::test_calibration_blocks_bind` | B | |
| B31 | Runtime wires `FixtureSource` (replay/demo) and `ManualEntrySource` (display) as the `OrderSource`s; `TICKET_BOUND` is only ever operator-sourced (a tap), never inferred | `[DEMO-CRITICAL]` | `22 § OrderSource`; `17 § Binding`; `ADR-0006` | bind reachable only via `POST /action` | B | |
| B32 | No network dependency anywhere in the safety core or `PROTOCOL_ONLY`; single-host, loopback-only; no egress path in `src/` | `[DEMO-CRITICAL]` | `24 § The line that is never crossed, § Threat model, § out of scope` | server binds `127.0.0.1`; grep `src/` for outbound HTTP | B | |
| B33 | FastAPI + WebSocket + a single vanilla HTML/CSS/JS page; no build step; **full** state pushed at 5Hz | `[DEMO-CRITICAL]` | `39 § 6` | `src/ui/server.py`; `tests/integration/ui/test_ws.py` | B | |
| B34 | `POST /action` → a worker event (`OPERATOR_ASSERTION` et al.) in the log; the UI **never** mutates `WorldState` directly | `[DEMO-CRITICAL]` | `22 § WorkerDisplay`; `10 § Feedback edges`; `26 § Worker actions` | `tests/integration/ui/test_actions.py`; grep `src/ui` for state mutation | B | |
| B35 | Routes: `/` worker display; `/inspector` read-only state + event timeline; inspector has no write route and reads the same projections as the runtime | `[DEMO-CRITICAL]` | `39 § 6`; `25 § Inspector` | HTTP 200 on both; route table has one `POST` | B | |
| B36 | Wire shape is A's `snapshot.schema.json`, frozen at T+90; the mock at `src/ui/static/mock/snapshot.json` is a verbatim copy of `snapshot.example.json`; no parallel shape invented | `[DEMO-CRITICAL]` | `22 § WorkerDisplay`; BUILD_B §1.5 | `tests/integration/ui/test_snapshot_shape.py` validates mock + live payload against the schema | B | |
| B37 | Tier 0 reset-prompt surface: checklist at `TICKET_BOUND`; items tick themselves off as resets are observed or asserted; auto-resolves when preconditions met; reads as help, not interrogation | `[DEMO-CRITICAL]` | `26 § Tier 0`; `16 § Lifecycle` | renders from mock; live in beats 3→4 | B | |
| B38 | Tier 1 interrupt surface: single line, dismissible, names the specific carrier and action, `[Swap tool] [Already swapped]`; shows its own resolution rather than silently clearing | `[DEMO-CRITICAL]` | `26 § Tier 1`; `16 § Lifecycle` | renders from mock | B | |
| B39 | Tier 2 hold surface: the only blocking surface; does not auto-clear; shows the evidence trace and the disclaimer *"This is an observation, not a determination. Confirm with the cook."*; `[Cook confirms tool was clean] [Remake]` | `[DEMO-CRITICAL]` | `26 § Tier 2`; `37 § beat 5`; `16 § Rules` | renders from mock | B | |
| B40 | Worker actions: Acknowledge, "Already swapped"/"Already clean", Dismiss (Tier 0/1 only), Remake, Resolve hold — each enters the log as an event; dismissal suppresses the display, never state | `[DEMO-CRITICAL]` | `26 § Worker actions, § What dismissal does`; `02 § Human responsibility` | `test_actions.py` one case per action | B | |
| B41 | Every alert type resolvable in ≤ 2 taps, **measured on a teammate, not the author** | `[DEMO-CRITICAL]` | `38 § P3 Gate`; `26 § constraint 3`; `34 § Secondary — UX` | measurement record (who, tier, taps) in `status_B.md` | B | |
| B42 | Glanceable in < 1s: headline ≤ 6 words rendered large; required actions as icon + noun; no dashboard on the worker surface; silence is the default state | `[DEMO-CRITICAL]` | `26 § Design constraints 1, 2, 4` | inspection of `/` with no alerts and with each tier | B | |
| B43 | Evidence-trace rendering: one line per `TraceStep` (time, type, participants, grade, delta) **plus absence lines**; rendered from the same derivation object as the alert | `[DEMO-CRITICAL]` | `25 § The evidence trace`; `38 § P3 scope` | trace pane renders the mock Tier 2 trace incl. the `-- no GLOVE_CHANGE observed --` line | B | |
| B44 | Live carrier grid: one tile per carrier showing **both axes** (taint set + epistemic status); tiles go amber as taint spreads; built in P3, not P6 | `[DEMO-CRITICAL]` | `37 § req 1`; `39 § 6`; `13 § Two independent axes` | beats 1–2 in replay | B | |
| B45 | **No green state anywhere** in the UI; no prohibited claim words ("safe", "contaminated", "clean because wiped", "allergen-free") in any UI copy B authors | `[DEMO-CRITICAL]` | `02 § Unsupported claims`; `40 § Session 3 prompt`; `39 § 6.2` | grep CSS for green hues; `tests/integration/ui/test_copy_lint.py` | B | |
| B46 | Never accusatory; no per-person feedback; no worker or customer identity rendered anywhere — the only worker field is `worker_slot` | `[DEMO-CRITICAL]` | `26 § constraints 5–6`; `24 § Structural guarantees`; `ADR-0010` | grep UI for name/id fields; copy review | B | |
| B47 | Mode banner: `PROTOCOL_ONLY` states plainly vision is unavailable; reasoning-down state visibly stated; `CALIBRATION` shows the specific config error | `[DEMO-CRITICAL]` | `10 § Runtime modes`; `23 § P5, P11, P12`; `37 § req 5` | render with each mode | B | |
| B48 | Inspector shows carrier states on both axes, zones + recent contacts, bound tickets + restrictions, active alerts with traces, mode + health (incl. rejected events and late rate), and a scrubbable event timeline | `[MUST]` | `25 § Inspector, § Log levels`; `19 § Ordering` | `/inspector` renders all panes from the live socket | B | |
| B49 | Aesthetic: industrial HMI — radius ≤ 3px; no box-shadows on data surfaces (1px borders); no gradients; monospace `tabular-nums` for every number/id; state as thick left borders + fills, not pills; uppercase wide-tracked micro-labels; dark ground that ignores viewer theme; **no charts**; four panes in dataflow order, worker surface at the bottom | `[DEMO-CRITICAL]` | `39 § 6.1, § 6.2`; `40 § Session 3 prompt` | CSS grep for `border-radius` > 3px, `box-shadow`, `gradient`; visual check | B | |
| B50 | Every UI asset vendored into `src/ui/static/` (Open Props, Phosphor SVG sprite, Archivo + IBM Plex Sans/Mono woff2 with real fallback stacks); no CDN, no component library, no Tailwind, no bundler, no Lucide; page loads with networking disabled | `[DEMO-CRITICAL]` | `39 § 6.1`; `24 § The line that is never crossed` | grep templates for `http`; offline load | B | |
| B51 | Presentation-layer suppression: one visible alert per station, highest tier wins | `[DEMO-CRITICAL]` | `16 § Alert identity — the anti-spam mechanism` (table row "Presentation") | render mock with several alerts → one primary surface | B | |
| B52 | Beat 0: four carriers shown, system silent | `[DEMO-CRITICAL]` | `37 § Script beat 0` | run book step 0 | B | |
| B53 | Beat 1: bind ticket 47 (no restriction) → **silence**; `gloves`, `spreader`, `board` turn amber | `[DEMO-CRITICAL]` | `37 § beat 1`; `03 § A` | replay of `scenarios/demo.yaml` (A) through B's UI | B | |
| B54 | Beat 2: reach into mayo → `bin:mayo` flips amber: *"Shared container — PINE_NUT — affects all future tickets"*; emerges as a plain pathway, not a special case | `[DEMO-CRITICAL]` | `37 § beat 2, § req 2`; `03 § I`; `14 § Pathway classes` | replay | B | |
| B55 | Beat 3: bind ticket 48 (PINE_NUT) → Tier 0 **before any motion** listing gloves / spreader / use sealed backup mayo | `[DEMO-CRITICAL]` | `37 § beat 3, § req 3` | replay | B | |
| B56 | Beat 4: glove change + spreader swap → checklist ticks itself, prompt clears, prep proceeds silently | `[DEMO-CRITICAL]` | `37 § beat 4, § req 4` | replay | B | |
| B57 | Beat 5: rerun 48 ignoring the prompt → **Tier 2 HOLD** with the full evidence trace and the disclaimer line | `[DEMO-CRITICAL]` | `37 § beat 5` | replay | B | |
| B58 | Beat 6 close; beats 3 and 6 survive with zero perception (`PROTOCOL_ONLY` presentable) | `[DEMO-CRITICAL]` | `37 § beat 6, § req 5` | `PROTOCOL_ONLY` run | B | |
| B59 | Fallback ladder: live perception → recorded log replay → `scenarios/demo.yaml`, **all producing identical UI**; camera-unplug drill rehearsed for real; run book written beat by beat | `[DEMO-CRITICAL]` | `37 § Determinism under pressure`; `38 § P6 gate` | rehearsal record in `status_B.md`; `data/build/runbook_B.md` | B | |
| B60 | **A complete demo runs in `PROTOCOL_ONLY` with no camera attached** (the P3 gate) | `[DEMO-CRITICAL]` | `38 § P3 Gate`; `39 § 9 The cut line` | run book executed end to end; `P3 GATE GREEN` posted | B | |
| B61 | Alert-copy lint green on B's copy: if A's lint fails on B-authored strings, fix the copy, never the lint | `[DEMO-CRITICAL]` | `38 § P3 Gate`; `02 § Unsupported claims`; `33 § Required negative tests` | A's lint + B's `test_copy_lint.py` | B | |
| B62 | `PerceptionPipeline.start/stop/health` writes only to `EventSink`; honesty obligations (`CARRIER_OBSERVABILITY_CHANGED`, `TRACK_IDENTITY_SUSPECT`, `HEALTH_DEGRADED`); occlusion-reporting recall ≥ 0.95; NR < 1.0/hr; contact-to-alert < 1.5s; frames never persisted by default | `[SHOULD]` | `22 § PerceptionPipeline`; `21 § Obligations of honesty`; `38 § P4/P5 gates`; `34 § Secondary`; `24 § guarantee 4` | **OPEN — no hardware** (`status_B.md`) | B | |
| B63 | `[DEFER]` authentication, TLS, RBAC, multi-tenancy, network transport — **not built** | `[DEFER]` | `38 § Explicitly defer`; `24 § Deliberately out of scope` | grep `src/` for `auth`, `jwt`, `login`, `ssl` → none | B | |
| B64 | `[DEFER]` mobile app, analytics, per-person metrics, manager surface, multi-station aggregation — **not built** | `[DEFER]` | `38 § Explicitly defer, § Could build`; `26 § constraint 6, § Escalation` | grep; route table | B | |
| B65 | `[DEFER]` POS/delivery adapters; evidence video clips / frame persistence; rag/wipe-cloth perception; wash-cycle detection; ML risk model — **not built** | `[DEFER]` | `38 § Explicitly defer`; `24 § A deliberate rejection`; `14 § Rag modeling` | grep `src/` for `<video`, `persist_frames`, `cv2` outside `perception/`; `orders/adapters/` holds only A's sources | B | |
